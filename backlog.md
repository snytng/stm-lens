# Stm Lens for astah* プロダクトバックログ

## 実装待ちタスク (To Do)
- [x] B17. **[🚨 CRITICAL BUG] テスト生成時のガード条件欠落バグの修正**: RecordおよびGenerate機能でテストスクリプトを自動生成する際、イベント名にガード条件（例: `[x > 0]` や `[else]`）が含まれず素のイベント名のみが出力されてしまう。これにより、ジャンクション等を含むモデルのテスト実行時（Run Script）に該当ボタンが見つからずエラーになる致命的な問題を修正する。
- [ ] B3. **フォーク（Fork）擬似状態の実装**: 1つの遷移を複数の並行リージョンへの遷移に分割するフォーク擬似状態に対応する。
- [ ] B4. **ジョイン（Join）擬似状態の実装**: 複数の並行リージョンからの遷移を同期し、全てが揃った時点で1つの遷移に統合するジョイン擬似状態に対応する。
- [ ] B7. **テストカバレッジ（N-Switch）の計測とテストケース自動生成**: シミュレーション実行時に遷移ペア（n-switch）のカバレッジを計測し、未達の遷移パスを自動的に実行するテストケースを生成・登録する機能を追加する。
- [ ] B9. **静的モデル検証（Lint）機能の実装**: シミュレーション実行前に、UMLの構文違反や論理的な問題（初期状態の欠落、到達不能な状態、非決定的な遷移など）を事前にチェックし、ユーザーに警告・エラーを提示する機能を追加する。
- [x] B10. **UIのコンパクト化（レイアウト整理）**: 画面の縦幅を節約し、図の表示領域を広げるためにボタンの配置を見直す。
- [x] B11. **テキストベースのテスト自動実行とアサーション機能**: テキスト形式のテストスクリプト（図の指定、イベント投入、期待される状態・ログのAssert）をUIに貼り付けて一括実行し、合否判定を行う機能を追加する。複数図の連続テスト（リグレッションテスト）にも対応する。
- [x] B12. **自動テストUIのレイアウト改善**: テキストベースの自動テスト機能について、スクリプト入力欄と結果表示欄が縦に並び画面を占有している問題を解決する。左右分割（JSplitPane）、IDE風の実行ボタンとステータス表示を採用し、よりコンパクトで使いやすいレイアウトに改善する。
- [x] B13. **テスト記録(T20)と自動テスト(T30)の統合**: UIでのシミュレーション記録（Record）を、T30のテキストテストスクリプト形式（`Target:`, `Fire:`, `AssertState:`）で自動生成・保存するように改修する。これにより、手動操作がそのままアサーション付きの回帰テストとして機能するようにし、テスト管理エンジンを一本化する。
- [x] B14. **Test Scriptクリアボタンの追加**: スクリプトエリアのテキストを頻繁に削除する手間を省くため、ワンクリックで内容を消去できるクリアボタン（ゴミ箱アイコン等）をUIに追加する。
- [x] B15. **テストロード時の上書き確認ダイアログの追加**: 保存済みのテストケースをドロップダウンから選択してロードする際、現在エディタに編集中のスクリプトが存在する場合は、誤上書きを防ぐ（および意図的な削除操作を省く）ための確認ダイアログを表示する。
- [x] B16. **テスト記録仕様の見直し（常時Generate化）**: 途中からRecordを開始するユースケースの必要性を再考し、「シミュレーションのStartと同時に常に操作が記録（自動Generate）される」仕様への変更を検討する。手書きを前提とした難易度の高い仕様から脱却し、誰でも直感的にテストを作れる体験へ洗練させる。
- [x] B18. **テストケース保存時の上書き確認ダイアログの追加**: `Save`ボタンでテストケースを保存する際、入力した名前のテストケースが既に存在する場合は、上書きして良いかの確認ダイアログを表示して誤上書きを防ぐ。
- [x] B19. **テストケースコンボボックスのキャンセル操作時の誤作動修正**: `Saved Tests`のコンボボックスで、項目を選択せずにポップアップを閉じた際（コンボボックス外をクリックした際など）に、意図せずテストロード処理が走り、エディタの上書き確認ダイアログが出てしまう問題を修正する。
- [x] B20. **プラグイン自身の状態遷移設計 (Mermaid)**: `design.md` に、本プラグインのUIやモード遷移（初期化、自動生成モード、保護モード、テスト実行中など）を定義したステートマシン図をMermaid形式で記述する。これにより、人間とAIの間の「状態（コンテキスト）」の認識のズレを防ぐ。
- [ ] B21. **ドッグフーディングによる自作ツールの設計検証**: B20で定義した本プラグインのステートマシン図をastah*上でモデリングし、本プラグイン自身を使ってシミュレーション・テストを行う。図の切り替え時などのエッジケースの振る舞い漏れを検出する。

## 開発プロセス・知見発信タスク (Future Milestones)
本プロジェクトの開発を通じて得られた課題から、今後の開発プロセス自体を改善するためのタスク。

- [ ] **P1. astah* API 探索・検証用スクリプト群の整備**: 今後複雑なUML要素（フォーク、ジョイン等）を実装する前に、APIがどのようなオブジェクト構造を返すかを実験・確認するための汎用的なスクリプト環境と、ドキュメント化のフローを構築する。必要であれば機能実装の手を止めて、この「知見を貯める活動」を優先する。

本プロジェクトで培った「人とAIが協働するための新しいソフトウェア開発プロセス」を世の中に発信し、エンジニアコミュニティと意見交換を行うための準備タスク。

- [ ] **F1. 発信に向けたコンテンツとテンプレートの準備 (with Gemini)**
    *   **フレームワークの命名**: このプロセスにキャッチーな名前をつける（例: *AI-Driven Documented Development (AIDDD)* や *Context-Sync Agile* など）。
    *   **記事の構成案作成**: ZennやQiitaで公開するためのストーリー構成を考える。
        *   *タイトルのイメージ*: 「AIにコードを書かせるのをやめたら、開発爆速になった話 〜AIとの『ドキュメント駆動・ペアプログラミング』の実践〜」
        *   *内容*: `current_task.md` によるコンテキスト管理や、`insights.md` による哲学の共有がどれほど重要だったかの体験談。
    *   **ルールの汎用化**: 現在の `development_rules.md` (astah*特化) を、どんなWeb・アプリ開発でも使える「汎用AI協働開発テンプレート」に書き換える。
    *   **OSS公開支援**: GitHubでテンプレートリポジトリとして公開するための準備（README等の整備）。
    *   **SNS発信準備**: 記事公開に合わせた X (Twitter) 等での発信文案の作成。
- [ ] **F2. Mermaid連携と Executable Specification (実行可能な仕様) の探求**: Mermaid形式のステートマシン図をastah*にインポートする機能、あるいはMermaidテキストを直接読み込んでシミュレーション・自動テストを実行するツールの構想・調査を行う。「テキストで書ける実行・検証可能な仕様書」としてエンジニアに大きな価値を提供するプロダクトへの進化を目指す。